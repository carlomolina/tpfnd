{% extends 'base.html.twig' %}

{% block body %}
    <h3>Welcome, {{ user.Firstname }}!</h3>
    <p>You are registered to tpfnd through your email {{ user.Email }}.</p>
    <br/>
    <a href="{{ path('user_edit', {'id': user.Id} ) }}">Edit Name</a> &nbsp&nbsp&nbsp&nbsp&nbsp
    <a href="{{ path('password_change', {'id': user.Id } ) }}">Change Password</a>&nbsp&nbsp&nbsp&nbsp&nbsp
    <a href="/logout">Logout</a>
{% endblock %}
